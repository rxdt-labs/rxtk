[project]
name = "rxtk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Christopher Sumnicht", email = "csumnicht@berkeley.edu" }
]
requires-python = ">=3.11"
dependencies = [
    "pypagate>=0.4.3",
]

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
bootstrap = [
    "ttkbootstrap>=2.0.1",
]
